# Séance 4 — Flutter Partie 2 — MAWANE (MII-BDCC, ENSET)

TP du module **Développement Mobile (Flutter / Dart)**, Pr. **MAWANE**,
Master MII-BDCC, ENSET — Université Hassan II Casablanca.

Exemples Dart vus en cours (POO : classes, héritage, polymorphisme, constructeurs nommés,
surcharge d'opérateur).

## Contenu

| Fichier | Notion |
|---------|--------|
| `bin/exemple1_heritage.dart` | Héritage et polymorphisme (`Personne` / `Salarie extends Personne`, `@override`). |
| `bin/exemple2_point.dart` | Constructeur nommé (`Point.origin()`), méthode `distance`, surcharge de l'opérateur `+`. |
| `bin/exemple3_rectangle.dart` | Classe `Rectangle` avec calcul de surface et périmètre. |

## Lancer

```
dart run bin/exemple1_heritage.dart
dart run bin/exemple2_point.dart
dart run bin/exemple3_rectangle.dart
```

## Dossier source local

`C:\Users\k.soumia\Desktop\App-Mobile\Seance4-FlutterPartie2`
